{
  "name": "copilot-usage",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Proxy locale che misura il consumo di token GitHub Copilot con attribuzione per progetto",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22.9"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b && tsc -p tsconfig.test.json",
    "test": "vitest run",
    "dev": "npm run dev -w @vidiemme/copilot-proxy",
    "smoke": "npm run smoke -w @vidiemme/copilot-proxy",
    "verify-detection": "npm run verify-detection -w @vidiemme/copilot-proxy",
    "verify-e2e": "npm run verify-e2e -w @vidiemme/copilot-proxy",
    "release": "npm run typecheck && npm test && npm publish -w @vidiemme/copilot-usage-contract && npm publish -w @vidiemme/copilot-proxy"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
